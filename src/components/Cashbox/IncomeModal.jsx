import { Modal } from '@mui/material';
import React from 'react';

const IncomeModal = () => {
  return (
    <Modal open={isModal}>
      <div>jgod</div>
    </Modal>
  );
};

export default IncomeModal;
